# OpenSpec — Project Index

> This file is the root index for all OpenSpec capability specs.
> Specs live in `openspec/specs/` as individual Markdown files.
> Each spec must have a Requirements section and a Scenarios section.

## Capabilities

<!-- Add capability entries here as specs are created:
- [cap-001-name](specs/cap-001-name.md) — short description
-->

## Conventions

- Each spec file: `openspec/specs/<cap-id>-<slug>.md`
- Change folders: `openspec/changes/<YYYY-MM-DD>-<slug>/`
  - `proposal.md`, `design.md`, `tasks.md`
- Archive when implemented AND smoke-tested: move to `openspec/archive/`
